import React from 'react';
import Header from '../Header/Header';

const Orders = () => {
    return (
        <div>
            <Header />
            <h1>Orders</h1>
        </div>
    );
};

export default Orders;